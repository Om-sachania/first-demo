// Searching in Array : 
    // arr.indexOf() method : 
    let x=[1,0,true];
    console.log(x.indexOf(0)); // 1
    console.log(x.indexOf(true)); // 2
    console.log(x.indexOf(null)); // -1 (as it is not present in the array)

// arr.includes() method : 
    console.log(x.includes(1)); // true

// arr.lastIndexOf() method : 
    console.log(fruits.indexOf("plum")); // 0 (First plum)
    console.log(fruits.lastIndexOf("plum")); // 2 (Last plum)